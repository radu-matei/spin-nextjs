{
  "name": "spin-next-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build && next export",
    "dev": "nodemon --watch content --verbose --legacy-watch --signal SIGINT --exec 'spin build && spin up --follow-all'"
  },
  "dependencies": {
    "next": "13.1.1",
    "react": "18.2.0",
    "eslint": "^8.30.0",
    "eslint-config-next": "13.1.1",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/node": "18.11.18",
    "@types/react": "18.0.26",
    "@types/react-dom": "18.0.10",
    "nodemon": "^2.0.20",
    "typescript": "4.9.4"
  }
}
